{
  "mcpServers": {
    "onshape": {
      "command": "npx",
      "args": ["-y", "@hedless/onshape-mcp-server"],
      "env": {
        "ONSHAPE_ACCESS_KEY": "${ONSHAPE_ACCESS_KEY}",
        "ONSHAPE_SECRET_KEY": "${ONSHAPE_SECRET_KEY}"
      },
      "description": "Onshape CAD integration - Requires API Keys (see .env.example)"
    },
    "numpy-mcp": {
      "command": "python",
      "args": ["-m", "numpy_mcp"],
      "description": "Scientific computation with NumPy - for aerodynamic calculations"
    },
    "mcp-server-fetch": {
      "command": "python",
      "args": ["-m", "mcp_server_fetch"],
      "description": "Fetch web content - for accessing airfoil databases"
    },
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@anthropic-ai/mcp-server-filesystem",
        "${PROJECT_ROOT}"
      ],
      "description": "Read/write files in your project directory"
    }
  }
}
