{
  "mcpServers": {
    "postgresql": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://localhost:5432/fpuna_education"
      ],
      "description": "Read-only PostgreSQL database access with schema inspection",
      "capabilities": ["sql_queries", "schema_inspection", "data_analysis"],
      "useCases": [
        "Query student data",
        "Analyze course statistics",
        "Inspect database schema",
        "Generate SQL reports"
      ],
      "note": "Configure connection string for your specific database"
    }
  }
}
