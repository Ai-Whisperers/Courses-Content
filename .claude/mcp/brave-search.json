{
  "mcpServers": {
    "brave-search": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-brave-search"
      ],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      },
      "description": "Web, image, and news search using Brave Search API",
      "capabilities": ["web_search", "image_search", "news_search", "privacy_focused"],
      "requirements": {
        "apiKey": "Get free API key from https://brave.com/search/api/ (2000 queries/month free)"
      }
    }
  }
}
