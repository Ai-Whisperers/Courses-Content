# Windsurf Cascade Rules

## 🧬 Core Guidelines

- **Precision**: Never start fixing bugs or adding new features unless explicitly told.
- **Exclusion**: Respect `.aiignore` and `.gitignore` at all times.
- **Context**: Check existing files before creating new ones to avoid duplication.

## ⌨️ Communication Style

- Use bullet points for plans.
- Always provide a brief rationale for major architectural changes.
- **Language**: English for code, Spanish for interactions.

## 🛠️ Tool Usage

- **Terminal**: Use `Turbo Mode` for automated command execution if safe.
- **Git**: Always check current branch before committing.
- **Search**: Use `Brave Search` for external documentation lookups.

<coding>
- Use strict TypeScript/Python types.
- Follow Clean Code principles (SOLID, DRY).
- Document complex Regex or math logic.
</coding>

<filesystem>
- Organize files by domain/feature.
- Use `recursos/` for assets and external data.
</filesystem>
